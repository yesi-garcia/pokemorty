const jwt = require('jsonwebtoken');

/**
 * se pasa el objeto del usuario
 * @param {*} user 
 */
const tokenSing = async(user) => { // firmar token

}

const verifyToken = async() => { // verificar token

}

module.exports = { tokenSing, verifyToken }